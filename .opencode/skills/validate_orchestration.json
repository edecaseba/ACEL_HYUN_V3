{
  "name": "validate_orchestration",
  "description": "Valida la orquestación completa de agentes: archivos, permisos, alineación y fallback.",
  "parameters": {
    "type": "object",
    "properties": {
      "scope": {
        "type": "string",
        "description": "Alcance de validación: 'all', 'files', 'permissions', 'fallback', 'alignment'"
      }
    },
    "required": ["scope"]
  }
}